github-actions.yml
Basic CI setup:

lint

test

build

test-workflow.yml
Runs pytest or Django tests automatically on PRs.

build-and-deploy.yml
Optional if deploying to:

Render

Railway

Docker Hub

GitHub Pages (frontend)